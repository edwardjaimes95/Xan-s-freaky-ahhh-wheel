# Shared wheel setup

Every device loads the same wheel from `wheel-options.json`.

To change it, open `wheel-options.json` in GitHub, press the pencil icon, edit the label/color/points, and commit the change. Point values stay hidden on the website until a section wins.
